{
  "name": "navee-stores",
  "version": "1.0.0",
  "private": true,
  "description": "NAVEE Stores multi-tenant e-commerce SaaS platform with frontend, backend, auth, tenant data isolation, and database integrity.",
  "scripts": {
    "dev": "node scripts/start-dev.js",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
